import type { CapacitorConfig } from "@capacitor/cli";
const config: CapacitorConfig = {
  appId: "es.tgd.app.educativa",
  appName: "TGD App Educativa",
  webDir: "dist",
  server: { androidScheme: "https" }
};
export default config;
